#include <stdio.h>

int main() {
    int n = 8;
    int a = 1, b = 1, c, i;
    printf("Deret Fibonacci %d angka:\n", n);

    for (i = 1; i <= n; i++) {
        printf("%d ", a);
        c = a + b;
        a = b;
        b = c;
    }
    return 0;
}
